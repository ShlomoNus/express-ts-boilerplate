{
    "ignore": ["**/*.test.ts", "**/*.spec.ts", ".git", "node_modules"],
    "watch": ["src"],
    "exec": "node --inspect -r tsconfig-paths/register -r ts-node/register",
    "ext": "ts,js",
    "env": {
        "NODE_ENV": "dev"
    }
}
